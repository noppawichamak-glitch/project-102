# 📦 Project 102: ระบบจัดการสินค้าและคลังสินค้า (Inventory & POS System)

โปรเจกต์นี้เป็นแอปพลิเคชัน Windows Forms ที่พัฒนาด้วย **C# (.NET 10.0)** สำหรับการจัดการข้อมูลสินค้า คลังสินค้า และระบบขาย/ออกใบเสร็จ (POS) โดยใช้ **SQLite** เป็นฐานข้อมูลหลัก และทำงานร่วมกับ **Dapper** (Micro-ORM) เพื่อประสิทธิภาพในการดึงข้อมูล พร้อมรองรับการออกใบเสร็จในรูปแบบ PDF ด้วย **QuestPDF**

---

## 🚀 เทคโนโลยีที่ใช้ (Technologies Used)

* **Framework:** .NET 10.0 (Windows Forms)
* **Language:** C#
* **Database:** SQLite
* **ORM:** Dapper (v2.0.123)
* **PDF Generator:** QuestPDF (v2024.2.0)

---

## ✨ ฟีเจอร์การทำงาน (Features)

### 1. ระบบจัดการสินค้า (Product Management)
* ✅ **เพิ่มสินค้าใหม่:** รองรับการกรอก รหัสสินค้า, ชื่อ, ราคา, จำนวน (Stock) และ หมวดหมู่
* ✅ **แก้ไขข้อมูลสินค้า:** อัปเดตรายละเอียดต่างๆ ของสินค้าได้แบบ Real-time
* ✅ **ลบสินค้า (Soft Delete):** การลบสินค้าจะทำการเปลี่ยนสถานะ (`IsActive = 0`) แทนการลบข้อมูลออกจากฐานข้อมูลจริง เพื่อป้องกันข้อมูลสูญหาย
* 🚧 **ค้นหาและกรองสินค้า:** (กำลังพัฒนา) ค้นหาสินค้าตามชื่อ รหัส หรือหมวดหมู่

### 2. ระบบคลังสินค้า (Inventory Management)
* ✅ **นำเข้าสินค้า (Stock In):** เพิ่มจำนวนสินค้าเข้าคลัง พร้อมบันทึกประวัติ
* ✅ **เบิกออกสินค้า/ขาย (Stock Out):** ลดจำนวนสินค้าในคลัง พร้อมระบบตรวจสอบว่า Stock เพียงพอหรือไม่
* 🚧 **แจ้งเตือนสต็อก (Low Stock Alert):** (กำลังพัฒนา) แจ้งเตือนเมื่อสินค้าใกล้หมด

### 3. ระบบขายและออกใบเสร็จ (POS & Receipt)
* ✅ **สร้างใบเสร็จ (Generate PDF):** ออกใบเสร็จรับเงินในรูปแบบ PDF ตามจำนวนและราคาสินค้าที่เลือก 
* ✅ **คำนวณราคาสุทธิ:** คำนวณยอดรวม (Total) ให้อัตโนมัติและสร้างไฟล์เอกสารลงในระบบ
* 🚧 **ระบบตะกร้าสินค้า (Shopping Cart):** (กำลังพัฒนา) รองรับการทำรายการหลายสินค้าในบิลเดียว

### 4. ระบบฐานข้อมูลและจัดการข้อผิดพลาด (Database & Error Handling)
* ✅ **SQLite Database:** เชื่อมต่อและสร้างตารางอัตโนมัติเมื่อเปิดโปรแกรม (`DatabaseConfig`)
* ✅ **Data Validation:** ตรวจสอบความถูกต้องของข้อมูลก่อนบันทึก เช่น ราคาและจำนวนต้องไม่ติดลบ, รหัสสินค้าห้ามซ้ำ (Unique Constraint)
* ✅ **Exception Handling:** แสดงข้อความแจ้งเตือนที่เข้าใจง่ายเมื่อเกิดข้อผิดพลาด (User-friendly Messages)

---

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

```text
project_102/
│
├── Models/
│   └── Product.cs               # โครงสร้างข้อมูลสินค้า (Id, Code, Name, Price, Stock, ...)
│
├── Repositories/
│   ├── ProductRepository.cs     # จัดการ CRUD Operations ของสินค้า (Add, Update, SoftDelete, GetAll)
│   └── InventoryRepository.cs   # จัดการการเพิ่ม/ลดสต็อกและประวัติ (Stock In/Out)
│
├── Services/
│   └── ReceiptService.cs        # จัดการการสร้างไฟล์ PDF ใบเสร็จรับเงิน
│
├── DatabaseConfig.cs            # ตั้งค่าและจัดการการเชื่อมต่อ SQLite
├── Form1.cs                     # หน้าจอ UI หลักและ Event Handlers
└── Program.cs                   # จุดเริ่มต้นการทำงานของโปรแกรม
